import React from 'react';

export const Input = (props) => {
    return (
        <div>
            <input/>
            <label>{props.title}</label>        

        </div>
        
    )
}

// export default Input